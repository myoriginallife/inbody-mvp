# Database (local: docker compose up -d)
DATABASE_URL="postgresql://inbody:inbody@localhost:5432/inbody"

# Auth session secret (use a long random string in production)
SESSION_SECRET="change-me-in-production"
